import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  button: {
    width: '5rem',
  },
  progressBarContainer: {
    width: '100%',
    height: '100%',
  },
  progressBar: {
    width: '100%',
    height: '1rem',
  },
}));
